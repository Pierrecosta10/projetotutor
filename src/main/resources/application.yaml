spring:
  application:
    name: projetotutor

  datasource:
    url: jdbc:postgresql://localhost:5432/curso
    username: postgres
    password: ""
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true